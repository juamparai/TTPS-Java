# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/mascotas_db
spring.datasource.username=root
spring.datasource.password=JavaContrasena
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Configuration
server.port=8080

# Context Path para todos los endpoints
server.servlet.context-path=/TTPS_Raimundi_Suner

# Swagger UI path (opcional, por defecto es /swagger-ui.html)
springdoc.swagger-ui.path=/swagger-ui.html
# API docs path (opcional)
springdoc.api-docs.path=/api-docs